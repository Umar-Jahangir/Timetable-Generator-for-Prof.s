# CHANGED FROM CRA: Next.js only exposes env vars prefixed NEXT_PUBLIC_
# to the browser bundle (CRA used REACT_APP_). Server-only env vars
# (none needed yet) would omit the prefix entirely.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
